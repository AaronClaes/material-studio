# material-studio

A node-based texture/image processing studio built with TanStack Start, React 19, TypeScript, Tailwind CSS v4, and shadcn/ui.

## Rules

Use shadcn/ui components (MCP available) for UI related tasks. First see if shadcn/ui has the component we need before creating it.

Use WebGPU for node processors when it makes sense.
